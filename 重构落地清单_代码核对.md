# AINovelTools 重构落地清单与代码核对

版本：V1.0  
核对日期：2026-08-20  
基线：现有仓库自带虚拟环境测试 `12 passed`

## 1. 核对结论

### 1.1 可以直接落地的决策

以下决策与现有代码结构一致，适合先做：

- 统一后台任务入口；
- 合并章节编辑器脏状态；
- 修复章节保存返回值；
- Prompt 确认支持取消；
- 一致性审校使用编辑器当前正文；
- 无 `vectorstore` 时跳过 Embedding；
- 批量扩写使用真实最低字数；
- 抽取 LLM/Embedding 参数构造。

### 1.2 可以落地，但必须先建立兼容层

- 全局配置与小说工程配置分离：当前 `config.json` 的 `other_params` 同时保存小说参数和路径，不能直接删除；需要迁移读取和回写兼容。
- `NovelProjectRepository`：当前各 UI 标签页直接 `os.path.join`、`read_file`、`save_string_to_txt`，必须先让仓储层兼容现有文本文件，再逐页迁移。
- `chapter_manifest.json`：旧工程没有清单，首次打开必须根据现有章节文件生成初始清单，并把无法判断的文件标为 `draft`，不能默认标为 `finalized`。
- `blueprint.json`：当前蓝图真实来源是 `Novel_directory.txt`，需要先解析迁移，文本文件继续作为兼容渲染产物。

### 1.3 当前决策与现有实现存在结构性冲突

- 大纲 34 步：当前 `novel_generator/architecture.py` 是固定的 4 个生成阶段，并通过 `partial_architecture.json` 续跑，不存在 `OutlineWorkflow` 或 34 步数据模型。不能直接把旧函数改名为工作流服务，需新增模型、迁移器和渲染器。
- 原子定稿：当前 `finalize_chapter()` 读取磁盘正文，只更新摘要和角色状态，并在文件提交前/后直接更新向量库；它无法实现正文、剧情线、清单和快照的一次提交，需新建定稿服务并保留旧函数适配入口。
- 多小说切换：当前 `filepath_var` 只是 UI 路径变量，启动时从 `config.json.other_params.filepath` 读取，没有工程对象、资源关闭和全量刷新流程，需要新增 `ProjectManager`。

## 2. 现有代码证据

| 规划决策 | 当前实现 | 证据 | 影响 |
| --- | --- | --- | --- |
| 统一任务控制器 | 每个 UI 函数自行创建线程 | `ui/generation_handlers.py:75,121,316,412,461,717,782` | 取消、互斥和按钮恢复无法统一 |
| Prompt 可取消 | 后台线程直接 `event.wait()` | `ui/generation_handlers.py:276` | 取消全局任务时线程可能永久等待 |
| 生成函数只返回文本 | `generate_chapter_draft()` 会写章节文件 | `novel_generator/chapter.py:595-599` | UI 会出现“已保存/未保存”状态不一致 |
| 统一草稿保存 | 定稿 UI 先写正文再调用定稿 | `ui/generation_handlers.py:386-390` | 长期状态失败会留下孤立正文 |
| 原子定稿 | 只对摘要和角色状态逐文件替换 | `novel_generator/finalization.py:95-96` | 没有清单、剧情线、快照和回滚事务 |
| 章节状态清单 | 通过目录文件名扫描 | `ui/chapters_tab.py:57-74` | 无法表示 stale、draft_modified、index_pending |
| 章节脏状态单一来源 | 章节查看页无保存快照/哈希 | `ui/chapters_tab.py:87-112` | 切换章节不会统一询问未保存修改 |
| 工程配置分离 | 小说参数在全局 `other_params` | `config_manager.py:99-113`, `ui/config_tab.py:212-232` | 多工程会串参数 |
| 蓝图结构化来源 | 直接增量写 `Novel_directory.txt` | `novel_generator/blueprint.py:91-179` | 无法安全范围替换和字段校验 |
| 大纲唯一源 | 直接生成 `Novel_architecture.txt` | `novel_generator/architecture.py:194-202` | 架构文本仍是最终写入目标 |
| 角色库虚拟“全部” | 真实 `角色库/全部` 目录和副本 | `ui/role_library.py:48-52,766-1002` | 重命名、删除和移动有大量特殊分支 |
| 日志轮转 | `app.log` 追加写 | `novel_generator/common.py:7-17` | 长期运行会无限增长 |

## 3. 阶段总览

以下阶段是完整重构范围。阶段必须按顺序推进；前一阶段未通过验收，不进入后一阶段。每个阶段都单独提交、单独测试，迁移阶段保留旧格式兼容代码。

| 阶段 | 名称 | 主要目标 | 本阶段允许改动 | 明确不做 | 阶段完成标志 |
| --- | --- | --- | --- | --- | --- |
| 0 | 基线与测试 | 建立可比较、可回滚的起点 | 测试工程、日志字段、测试夹具、备份 | 不改变业务行为和文件格式 | 原程序可启动，基线测试可重复 |
| 1 | 已确认问题修复 | 修复当前直接影响功能的问题 | 草稿保存、脏状态、Prompt 取消、审校正文、Embedding 判断、批量字数、保存返回值 | 不引入 `project.json`、不改定稿事务、不改大纲/蓝图格式 | 关键操作可取消，保存状态正确，测试通过 |
| 2 | 基础逻辑统一 | 消除重复线程、模型参数和文件访问 | `TaskController`、模型配置辅助函数、Repository 第一版、日志轮转 | 不实现完整多工程切换，不删除兼容模块 | UI 不再自行创建重复任务，文件访问开始收口 |
| 3 | 工程管理 | 实现多小说工程隔离和完整切换 | `project.json`、`ProjectManager`、全局最近工程、迁移读取、切换刷新 | 不引入章节事务和大纲新模型 | A/B 工程切换不串数据，重启可恢复 |
| 4 | 章节生命周期 | 建立章节清单、状态机和统一上下文 | `chapter_manifest.json`、状态转换、连续性校验、`ChapterContextBuilder` | 不修改蓝图真实数据结构，不删除旧章节函数 | 只能连续新增，旧章修改能标记后续失效 |
| 5 | 蓝图与分卷服务 | 将蓝图和分卷变成可校验的结构化数据 | `volume_plan.json`、`blueprint.json`、范围生成、字段校验、安全替换 | 不实现原子定稿，不删除目录文本兼容产物 | 越界、缺章、重章和坏字段均不能覆盖原数据 |
| 6 | 大纲唯一状态源 | 让工作流成为大纲唯一真实来源 | `OutlineWorkflow`、34 步迁移、自定义步骤、AI 快速导入、架构渲染 | 不在迁移完成前删除 `architecture.py` 或兼容逻辑 | 删除架构文本后可从工作流重建 |
| 7 | 统一章节服务 | 统一单章、批量和旧章重写流程 | `ChapterService`、统一 Prompt 上下文、草稿生命周期、批量复用单章服务 | 不处理角色库目录迁移和 WebDAV | 单章与批量使用同一套规则和上下文 |
| 8 | 原子定稿 | 使正文和长期状态一次提交 | `FinalizationService`、`write_many()`、状态快照、正文哈希、stale 重建、索引待处理 | 不改变向量库格式，只在事务后更新索引 | 状态失败不留下孤立正文，重复定稿幂等 |
| 9 | 角色库、技能与 WebDAV | 收拢外围功能，移除 UI 中的存储/网络职责 | 角色库虚拟“全部”、原子角色保存、技能工程选择、WebDAV 服务化 | 不在迁移前删除旧角色副本和配置字段 | 角色无孤立副本，配置恢复后 UI 完整同步 |
| 10 | 删除旧代码与拆分 | 清理不可达入口并恢复模块边界 | 删除旧 UI 入口、拆分大文件、统一命名导入、静态检查 | 不删除仍承担旧工程迁移的兼容代码 | 完整回归通过，无重复业务实现 |

### 阶段之间的依赖

```text
阶段0 基线
  -> 阶段1 直接修复
  -> 阶段2 任务/配置/存储基础
  -> 阶段3 工程管理
  -> 阶段4 章节状态与上下文
  -> 阶段5 分卷与蓝图
  -> 阶段6 大纲唯一源
  -> 阶段7 统一章节服务
  -> 阶段8 原子定稿
  -> 阶段9 外围功能精简
  -> 阶段10 删除旧代码
```

### 当前执行范围

当前分支只执行阶段 0 和阶段 1。阶段 1 的具体改动为：

1. 修复 `generate_chapter_draft()` 的隐式保存；
2. 合并章节编辑器脏状态并统一未保存确认；
3. 让 `save_current_chapter()` 明确返回成功/失败；
4. 让 Prompt 确认等待响应取消令牌；
5. 让一致性审校读取编辑器当前正文；
6. 无 `vectorstore` 时不要求 Embedding；
7. 一致性审校不验证 Embedding；
8. 将批量扩写阈值改为真实最低字数；
9. 增加针对上述行为的回归测试。

阶段 1 完成前不做：`project.json`、`ProjectManager`、`ProjectRepository`、`chapter_manifest.json`、`blueprint.json`、`OutlineWorkflow`、原子定稿和角色库目录迁移。

## 4. 推荐目标模块

先新增模块，不立即拆除旧模块：

```text
domain/
├─ chapter_state.py       # 状态枚举、合法转换、清单记录
├─ models.py              # ProjectConfig、Blueprint、ChapterContext
└─ validation.py          # 配置、蓝图、事务数据校验
services/
├─ task_controller.py
├─ project_manager.py
├─ project_repository.py
├─ model_config.py
├─ outline_service.py
├─ blueprint_service.py
├─ chapter_context.py
├─ chapter_service.py
├─ finalization_service.py
├─ role_service.py
└─ webdav.py
storage/
├─ atomic_writer.py
└─ migrations.py
```

旧 UI 和 `novel_generator` 函数先作为适配层调用新服务，避免一次性改变所有调用方。

## 5. 分阶段代码改动清单

## 阶段 0：建立基线和边界

### 改动

1. 新增 `tests/fixtures/` 测试工程，包含空工程、旧工程、缺章工程、断档工程和有向量库工程。
2. 新增 `tests/test_chapter_continuity.py`，覆盖 `1,2,3,5,6 -> max_contiguous=3`。
3. 新增 `tests/test_atomic_writer.py`，模拟临时文件写入失败和替换失败。
4. 新增 `tests/test_prompt_cancel.py`，验证取消事件不会永久阻塞。
5. 在不改变行为的前提下记录任务 ID、工程路径和章节号。
6. 为所有新 JSON 文件约定 `version` 字段和 UTF-8 编码。

### 退出条件

- 现有测试仍通过；
- 可在临时工程完成架构、蓝图和一章草稿生成；
- 测试不读用户真实工程和真实 API Key。

## 阶段 1：修复已确认错误

### 1.1 统一模型配置读取

新增 `services/model_config.py`：

```python
get_task_llm_config(config, task_key) -> dict
llm_call_kwargs(config) -> dict
get_task_embedding_config(config) -> dict
```

迁移 `ui/generation_handlers.py` 中架构、蓝图、草稿、定稿、审校、批量生成的重复字段拆解。先保留旧函数参数签名，适配层负责传参。

### 1.2 修复章节草稿保存

- 修改 `novel_generator/chapter.py:526` 的 `generate_chapter_draft()`：只生成并返回文本，不写 `chapter_N.txt`。
- 在 `ui/generation_handlers.py:123` 的生成完成回调中，只把文本显示到编辑器，并更新 `_loaded_chapter_number` 与 `_chapter_saved_text`。
- 新增 `ChapterService.save_draft()`，先使用现有 `save_string_to_txt()`，后续再切换 Repository。
- `save_current_chapter()` 必须返回 `True/False`，写入失败不得更新已保存文本。

### 1.3 合并章节脏状态

在 `ui/main_window.py` 初始化：

```python
self._loaded_chapter_number = None
self._chapter_saved_text = ""
```

在 `ui/chapters_tab.py:87-112`：

- 加载成功后记录章节号和磁盘正文；
- 新增 `is_chapter_dirty()`；
- 切换、关闭、定稿、生成其他章节和批量生成前调用统一 `confirm_unsaved_chapter()`；
- 保存成功后刷新保存文本，并把清单状态设为 `draft` 或 `draft_modified`。

### 1.4 修复 Prompt 取消

改 `ui/generation_handlers.py:276`：

```python
while not event.wait(0.1):
    if cancel_event.is_set():
        raise TaskCancelled()
```

窗口取消、窗口关闭和全局取消都必须执行同一回调；取消后不得写章节文件，任务控制器必须恢复按钮状态。

### 1.5 审校当前编辑器正文

检查 `ui/generation_handlers.py:414` 的 `do_consistency_check()`，正文参数必须来自 `self.chapter_result` 或当前章节编辑器，而不是重新从章节文件读取。审校接口保留纯函数形式，不写正文、不写摘要、不写角色状态。

### 1.6 Embedding 条件与批量字数

- `novel_generator/chapter.py:431` 只有 `vectorstore` 目录存在且有效时才创建 Embedding adapter；目录不存在时直接返回“无知识库上下文”。
- `novel_generator/finalization.py:99` 向量索引更新移到文件事务成功之后；索引失败标记清单，不使定稿失败。
- 将 `ui/generation_handlers.py:334` 和 `:669` 的 `0.7 * word_number` 改为 `word_number`，并把提示文字从“70%”改为“最低字数”。

### 阶段 1 验收

- `\.venv\Scripts\python.exe -m pytest -q`；
- Prompt 取消在 1 秒内结束；
- 未保存正文能直接被审校；
- 无 `vectorstore` 且无 Embedding 配置时可以生成草稿；
- 生成草稿后 UI 仍显示未保存，显式保存后才变为已保存。

## 阶段 2：统一后台任务和文件访问

### 2.1 `TaskController`

新增 `services/task_controller.py`，提供：

```python
run(task_id, worker, button=None, cancellable=True,
    on_success=None, on_error=None, on_finally=None)
cancel(task_id=None)
wait_for_idle(timeout)
is_running(task_id=None)
```

先迁移 `generation_handlers.py` 的 7 个线程入口，再迁移 `config_manager.test_*` 和 `other_settings.py` 的 WebDAV 线程。`worker` 不得直接更新 Tk 控件；主线程回调统一使用 `master.after()`。

### 2.2 Repository 第一版

新增 `services/project_repository.py`，先覆盖现有格式：

```python
read_text(name)
write_text(name, content)
read_json(name, default)
write_json(name, value)
read_chapter(number)
write_chapter(number, content)
list_chapters()
path_for(name)
```

所有路径必须通过 `realpath` 校验在工程根目录下。第一版仅迁移 `chapters_tab.py`、`summary_tab.py`、`character_tab.py`、`directory_tab.py`、`setting_tab.py`，不同时改变文件格式。

### 2.3 日志

在 `novel_generator/common.py` 使用 `RotatingFileHandler`，限制文件大小和备份数量；默认只记录 LLM 输入输出长度，调试模式才保存原文，并统一补充 `task_id/project_id/chapter_number`。

### 阶段 2 验收

- `rg "threading.Thread" ui novel_generator` 只剩任务控制器内部和明确兼容代码；
- UI 标签页不再直接调用 `open`、`os.path.join`、`save_string_to_txt`；
- 单章、批量和 WebDAV 使用同一任务控制器；
- 静态检查无新增未使用导入。

## 阶段 3：工程配置和多工程切换

### 3.1 数据结构

新增 `project.json`：

```json
{
  "version": 1,
  "name": "小说A",
  "topic": "",
  "genre": "",
  "num_chapters": 120,
  "word_number": 3000,
  "current_chapter": 1,
  "planning_guidance": "",
  "chapter_guidance": "",
  "blueprint_mode": "range",
  "volume_mode": "manual_count",
  "volume_count": 0,
  "current_volume": 1,
  "selected_skill_ids": []
}
```

全局配置新增 `current_project` 和 `recent_projects`。保留 `other_params` 迁移读取，但新保存入口不得继续写入小说参数。

### 3.2 `ProjectManager`

新增：

```python
create_project(path, initial_config)
open_project(path)
switch_project(path, confirm_unsaved, cancel_tasks)
close_project()
```

`open_project()` 负责创建 Repository、执行未完成事务恢复、迁移旧格式、加载清单和刷新资源。`switch_project()` 在任务退出前不得修改 `filepath_var`。

### 3.3 UI 改动

- `ui/main_window.py:56,132-154` 改为从 ProjectManager 获取当前工程配置；
- `ui/config_tab.py:212-232,800-837` 将小说参数保存改为 `project.json`；
- `ui/novel_params_tab.py` 的路径输入改成创建/打开工程入口；
- 切换成功后重载架构、蓝图、章节、摘要、角色库和知识库，清空旧工程 UI 缓存。

### 阶段 3 验收

临时目录创建 A、B 两个工程，分别设置题材、章节数和章节正文；来回切换后所有内容隔离；重启后恢复当前和最近工程；切换期间有未保存正文或运行任务时按规则保存、放弃、取消或阻止切换。

## 阶段 4：章节清单、状态机和上下文

### 4.1 清单迁移

新增 `domain/chapter_state.py` 和 `chapter_manifest.json`。首次打开旧工程时扫描 `chapters/chapter_N.txt`：

- 空文件 -> `draft`；
- 有正文但无旧定稿记录 -> `draft`；
- 不能证明已定稿的文件不得标为 `finalized`。

新增 `find_max_contiguous_chapter()`、`validate_target_chapter()` 和合法状态转换测试。

### 4.2 章节上下文

新增不可变 `ChapterContext` 和 `ChapterContextBuilder`。将 `novel_generator/chapter.py:285-524` 中的文件读取、蓝图解析、摘要、角色和知识库检索拆出；旧 `build_chapter_prompt()` 先调用 Builder，再渲染现有 Prompt 模板。

### 4.3 单章与批量

将 `ui/generation_handlers.py:462-717` 的批量逻辑改为调用 `ChapterService.generate_draft()`、`save_draft()`、`finalize()`，删除批量独有的正文写入和参数拆解。

### 阶段 4 验收

磁盘有 1、2、3、5、6 章时只允许新增第 4 章；允许重写 1-3 章；任一失效章节存在时禁止新增；单章和批量构建出的上下文字段一致。

## 阶段 5：蓝图与大纲真实源

### 5.1 蓝图

新增 `blueprint.json` 读写和 `BlueprintService`：

- `parse_legacy_directory(text)` 将 `Novel_directory.txt` 转为结构化章节；
- `validate_range(start, end, total)`；
- `validate_chapters(fields, numbers)`；
- `replace_range(existing, start, end, replacement)`；
- 通过 Repository 原子保存 JSON，再渲染 `Novel_directory.txt`。

保留 `chapter_directory_parser.py` 作为迁移和兼容解析器，不能直接删除。

### 5.2 大纲

当前 `architecture.py` 的 4 阶段生成不能直接映射 34 步。新增 `outline_workflow.json`、`OutlineWorkflow`、`OutlineService` 和 `ArchitectureRenderer`：

- 旧 `partial_architecture.json` 迁移为固定步骤的草稿内容；
- AI 快速结果按稳定 `step_id` 导入为 `draft`；
- 架构生成只读取工作流并输出 `Novel_architecture.txt`；
- `ui/setting_tab.py:36-55` 的直接架构编辑先改为“编辑工作流步骤”或暂时只读；
- 迁移完成前保留 `novel_generator/architecture.py`。

### 阶段 5 验收

删除 `Novel_architecture.txt` 或 `Novel_directory.txt` 后可从 JSON 重建；范围生成不会覆盖范围外章节；模型输出字段缺失时保留原文件并报告具体错误；未完成全部 34 步仍可生成架构。

## 阶段 6：原子定稿与状态快照

### 6.1 新定稿服务

新增 `services/finalization_service.py`：

```python
finalize(chapter_number, chapter_text) -> FinalizationResult
```

执行顺序：

1. 校验连续性、正文非空、清单状态和旧章影响；
2. 读取前一章 `chapter_states/chapter_N-1.json`；
3. 生成摘要、角色状态、剧情线；
4. 拒绝空响应、错误结构或异常长响应；
5. 计算正文 SHA-256；
6. 使用 Repository `write_many()` 提交正文、三类长期状态、清单和当前快照；
7. 事务成功后更新向量索引；失败则把清单标为 `index_pending`。

### 6.2 旧函数适配

`novel_generator/finalization.py:37` 的 `finalize_chapter()` 暂时保留，但只负责把旧参数转换为服务请求，禁止继续独立写摘要和角色状态。`ui/generation_handlers.py:318-412` 和批量入口都调用新服务。

### 6.3 旧章重定稿

重新定稿第 N 章时，将 N+1 至最后一个已定稿章标为 `stale`，保存正文和新快照后禁止继续生成；提供“逐章重定稿”和“从快照重建状态”两个明确入口。

### 阶段 6 验收

- 任一长期状态生成失败，正文不单独提交；
- 同一正文哈希重复定稿不重复调用状态生成；
- 旧章重定稿后后续章为 `stale` 且禁止新增；
- 向量失败不影响正文和长期状态；
- 任一已定稿章可以读取对应状态快照。

## 阶段 7：角色库、技能和 WebDAV

### 7.1 角色库

`ui/role_library.py` 当前在 `:48-52` 创建真实“全部”目录，在 `:766-1002` 维护副本。迁移步骤：

1. 扫描“全部”副本，按内容哈希匹配真实分类文件；
2. 无法匹配的文件移动到 `未分类` 并记录报告；
3. 将“全部”改为内存聚合视图；
4. 新增 `RoleLibraryService.save_role(old_name, new_name, category, content)`；
5. 所有角色写入改用原子保存，确认迁移成功后再删除副本逻辑。

### 7.2 技能

技能库保留全局位置；选中技能 ID 写入 `project.json`。切换工程时从工程配置恢复，不再只存运行内存。

### 7.3 WebDAV

将 `ui/other_settings.py:167-336` 的 `WebDAVClient` 移至 `services/webdav.py`。备份和恢复对象明确为全局配置；恢复后调用统一配置刷新方法，不能只替换 `self.loaded_config`。

## 阶段 8：删除与拆分

只有在迁移、测试和回归完成后，才执行：

- 删除 UI 直接写核心文件的旧入口；
- 删除架构整篇覆盖入口；
- 删除“全部”副本同步分支；
- 删除确认无动态引用的 `ui/helpers.py`；
- 将 `generation_handlers.py` 拆为任务、架构、蓝图、章节、知识库处理器；
- 将角色库的文件操作、业务规则和 UI 拆开。

每次删除前执行 `rg` 引用扫描、运行迁移测试和完整回归，单独提交。

## 6. 需要调整或暂缓的规划决策

1. **蓝图真实源应改为 `blueprint.json`。** 原规划只明确了 `Novel_directory.txt`，但范围替换和字段校验要求结构化数据；本清单增加 JSON 源文件是必要调整。
2. **初始旧章节不能自动视为 finalized。** 现有代码没有可靠定稿记录，只能标为 `draft`，否则会伪造状态快照。
3. **原子定稿不能复用当前 `finalize_chapter()`。** 该函数签名依赖大量 UI 模型和 Embedding 参数，且自身读磁盘正文；必须新增服务并保留兼容适配。
4. **工程切换不能只改 `filepath_var`。** 必须先完成任务退出和 UI 脏状态处理，再更换当前工程对象。
5. **34 步工作流不应在阶段 1 实现。** 当前架构只有 4 个硬编码阶段，阶段 1 先修复稳定性，阶段 5 再迁移大纲模型更可控。
6. **日志轮转应提前到阶段 2。** `common.py` 当前全局追加 `app.log`，延后会让后台任务重构继续放大日志问题。

## 7. 推荐提交顺序

```text
test: 建立重构基线和测试工程
fix: 修复草稿保存与章节脏状态
fix: 支持 Prompt 取消和可选 Embedding
refactor: 统一模型配置读取
refactor: 引入 TaskController
refactor: 引入 ProjectRepository 兼容旧格式
feat: 增加 project.json 和旧配置迁移
feat: 实现 ProjectManager 工程切换
feat: 增加 chapter_manifest 和章节状态机
feat: 增加 blueprint.json 和范围校验
refactor: OutlineWorkflow 成为大纲唯一源
feat: 增加 ChapterContextBuilder
refactor: 单章和批量共用 ChapterService
feat: 实现定稿事务和状态快照
refactor: 角色库“全部”改为虚拟视图
refactor: WebDAV 移出 UI
chore: 删除确认无引用的旧入口
```

## 8. 第一批实际编码顺序

建议立即执行的最小批次：

1. 新增 `TaskCancelled`、`TaskController` 的最小实现和测试；
2. 修改 Prompt 等待取消；
3. 修改 `generate_chapter_draft()` 不再写文件；
4. 合并章节保存状态并让 `save_current_chapter()` 返回布尔值；
5. 修复审校正文来源；
6. 修复 Embedding 条件和批量字数阈值；
7. 抽取模型配置读取；
8. 运行测试和手工验证；
9. 再开始 Repository，不与第一批混改工程格式。

## 9. 当前可验证命令

```powershell
.\.venv\Scripts\python.exe -m pytest -q
rg -n "threading\.Thread|event\.wait\(\)|save_string_to_txt|open\(.*['\"]w" ui novel_generator
rg -n "finalize_chapter\(|generate_chapter_draft\(" ui novel_generator
```

静态搜索结果在迁移期间允许存在，但每个阶段应减少旧入口数量，并在提交说明中记录剩余兼容调用及删除条件。
