# R3 第一批阶段验收记录

## 前端展示 UI 固化标准
- 前端展示 UI 在不影响性能的前提下，必须美观、清晰、易用。
- 首次实现功能时确定布局、信息层级、视觉规范、控件规范、完整状态和响应式行为，形成稳定基线。
- 后续功能优先复用既有 UI；除非有明确的可用性、性能或业务依据，不进行大规模改版。
- UI 调整须保持操作路径和数据语义连续，并完成桌面与窄屏检查。

日期：2026-08-27  
状态：代码闭环完成，真实云端 API 冒烟待执行

## 已实现

- `ModelProfile`：明确区分 `CHAT` 与 `EMBEDDING`；DeepSeek/OpenAI 用于文本任务，硅基流动用于 Embedding。
- OS Secret Store：API Key 写入系统凭据库，项目库只保存 `SecretRef`。
- Chat ModelGateway：DeepSeek、OpenAI API 和通用兼容接口，支持非流式、SSE 流式、取消、超时、最多 3 次有限重试和稳定错误码。
- EmbeddingGateway：调用硅基流动 `/embeddings` 并校验返回向量；不建立向量索引和检索。
- `ContextAssembler`：当前章节、选区、章节规划、TokenBudget、上下文版本和 R4 实体占位来源。
- AI 任务合同：四个动作分别固定专业角色、目标对象、权限、禁区、验收标准、不确定性策略和纯文本候选输出合同。
- 上下文权威层级：采用 `P0-P6` 分区并记录来源数量、实际字符数和截断状态；模型推测不得成为项目事实。
- 检索上下文契约：已定义知识分块、Embedding 模型身份、来源证据和按需检索计划；尚未建立索引或真实召回。
- `AiProposal`：任务、目标正文修订、上下文版本、Prompt 版本、原始输出和审核状态持久化。
- Proposal 全部接受、部分接受和拒绝；接受只修改编辑草稿，不直接创建正文修订。
- 续写、重写选区、润色选区和章节摘要四个首批动作。
- Migration 7：`model_profiles`、`ai_tasks`、`ai_proposals` 及 Proposal 不可变身份触发器。
- Migration 8：增加模型能力字段，已有硅基流动 Profile 自动迁移为 `EMBEDDING`。
- Migration 9：`ai_tasks` 保存任务合同和上下文分区审计元数据，不保存完整 Prompt、正文或检索原文。
- 章节工作区已固定为四页签，规划树与节点详情采用响应式左右分栏。

## 自动化验收结果

- Rust workspace：26 个单元/集成测试通过。
- 网关模拟：非流式、流式、取消、超时和错误码映射通过。
- 数据安全：失败任务无 Proposal；接受 Proposal 后正文修订仍为空；Proposal 原始输出不可更新。
- ContextAssembler：相同输入产生相同上下文版本；重写缺少选区会被拒绝。
- 检索证据：校验来源身份，按相关度排序并按分块 ID 去重；Prompt 只包含命中的原文，不包含向量。
- 任务审计：任务合同和分区统计可从 SQLite 追溯，审计 JSON 不复制章节规划或正文内容。
- 前端 Vitest：通过。
- TypeScript 与 IPC 契约检查：通过，29 个客户端调用已校验。
- 前端生产构建：通过。
- `cargo fmt --check`：通过。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。

## 尚待人工验收

真实云端 API 冒烟步骤已统一迁移到 `doc/后续流程/R3延期项.md`。完成该清单前，FeatureCatalog 保持 `PARTIAL`。

## 下一流程门禁

进入 R4 前必须阅读 `doc/后续流程/README.md`，并复核其中指定的全部延期清单。未完成复核，不得开始下一流程开发。
