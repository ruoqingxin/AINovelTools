# R0-R2 P0 验收报告

## 前端展示 UI 固化标准
- 前端展示 UI 在不影响性能的前提下，必须美观、清晰、易用。
- 首次实现功能时确定布局、信息层级、视觉规范、控件规范、完整状态和响应式行为，形成稳定基线。
- 后续功能优先复用既有 UI；除非有明确的可用性、性能或业务依据，不进行大规模改版。
- UI 调整须保持操作路径和数据语义连续，并完成桌面与窄屏检查。

日期：2026-08-27

## 已通过

- Rust workspace 测试通过。
- `cargo clippy --workspace --all-targets -- -D warnings` 通过。
- 前端 Vitest 测试通过。
- IPC 客户端契约检查脚本已加入 `pnpm check`。
- FeatureCatalog 和无项目健康状态已提供。
- 规划更新提供 `expected_version` 并发检查。
- 正文 JSON 根结构校验、稳定 block ID 和 SHA-256 已提供。
- 正文及规划历史表增加禁止 UPDATE/DELETE 触发器。
- RecoveryLog 持久化保存和查询命令已提供。
- 规划节点移动、循环检测和归档子节点处理已提供。
- 正文提供基于稳定 block ID 的基础三方合并接口。
- 编辑器提供未保存切换拦截和浏览器关闭提示。
- 项目级恢复候选查询和全局提示已接入。
- 三方合并冲突检查及冲突块提示已接入正文工作区。

## 非阻塞限制

- `plan_nodes` 当前状态表仍保留兼容投影用途，后续可进一步收敛为从 `PlanRevision` 重建。
- 三方合并已提供 block 级后端接口和冲突块提示，复杂的逐块自动拼接策略保持人工确认。
- 真实桌面启动冒烟和故障注入测试需要在目标桌面环境执行，本地构建和 Rust/SQLite 演练已通过。
- 生产前端构建已通过（`vite build`）。

## P0 结论

P0/P1 的核心数据安全、并发、恢复、移动、差异和契约基础已经落地，R0-R2 代码验收通过，可以进入 R3。桌面人工冒烟作为持续验收项保留，不阻塞 R3 开发。
