# R4 延期与收尾复核清单

## 文档状态

- 复核日期：2026-09-02
- 复核范围：R0-R2、R3 和长期按需清单在 R4 结束时的状态
- 规则：每项只能标记为 `本阶段实现`、`继续延期` 或 `取消`
- 证据约定：用户在桌面端完成的真实云端操作记录为人工证据；仓库测试、构建和静态检查记录为自动化证据
- 用户决定：2026-09-02 将真实云端动作和 Tauri 宽窄屏截图继续延期，不阻塞 R5 首批设计与实现

## R4 剩余人工门禁

| 项目 | 状态 | 证据/下一步 |
|---|---|---|
| Chat 连接测试 | 本阶段实现 | 用户于 2026-09-02 确认已配置并测试通过 |
| Embedding 连接测试 | 本阶段实现 | 用户于 2026-09-02 确认已配置并测试通过 |
| 续写、重写、润色、摘要真实云端调用 | 继续延期 | 需在桌面端使用已配置 Key 各执行一次，并保存 Proposal 记录 |
| 取消生成且正文/修订历史不变 | 继续延期 | 需在桌面端执行一次可取消的真实流式任务并核对章节历史 |
| 鉴权失败且不污染正文 | 继续延期 | 需使用临时错误 Key 执行一次，完成后立即删除该 Key |
| 接受/部分接受 Proposal 后仅改草稿 | 本阶段实现 | Rust 集成测试已覆盖；桌面端人工复核随真实云端冒烟一起执行 |
| 桌面端宽屏与窄屏截图回归 | 继续延期 | 用户决定暂缓；代码和 Vitest 已通过，真实 Tauri 截图待后续补证 |

## R0-R2 延期项复核

| 延期项 | 决定 | 依据 |
|---|---|---|
| Rust DTO 自动生成 TypeScript | 继续延期 | 当前 typed facade、契约检查和 `tsc` 足以覆盖现有规模；DTO 数量或漂移风险达到门槛后再引入生成器 |
| 规划树独立路由 | 继续延期 | 当前章节工作区页签已承载规划树，尚无独立导航需求 |
| 统一 ProjectSettings 编辑界面 | 本阶段实现（基线） | Migration 10 已建立项目设置表和安全默认值；完整编辑 UI、写作风格与隐私字段仍列为 R5 前置增强 |
| 最近项目列表 | 本阶段实现 | 作为应用级状态保存在工作区入口，不写入项目数据库 |
| 完整 CrashMarker、迁移中断检测、诊断包 | 本阶段实现 | R4 I 阶段代码、隐私排除测试和诊断 IPC 已完成 |
| 真实 Tauri 启动/窗口失败诊断 | 继续延期 | 依赖真实桌面运行环境，保留为人工门禁，不以单元测试代替 |
| RecoveryLog 压缩和后台清理 | 本阶段实现 | 已纳入启动恢复和后台任务实现 |
| 完整备份、哈希校验、恢复到新目录 | 本阶段实现 | 真实临时目录和 SQLite 恢复演练已通过 |
| 大型规划树虚拟化和查询分页 | 继续延期 | 当前数据规模未达到引入门槛；搜索已有基础 `limit/offset` 分页 |

## 长期能力复核

以下能力均为 `继续延期`，目标阶段和触发门槛仍以 `长期按需能力与延期项.md` 为准：

- Tantivy、向量索引、混合检索和高级 ContextAssembler。
- Relation/Fact/Event/Belief/Foreshadowing 之外的复杂知识治理扩展。
- 完整 Task Runtime、Lease、Checkpoint、ResourceGovernor、独立 Worker 和 Workflow Runtime。
- 长章节按场景生成、多角色审核、多窗口治理、导入导出和项目交换包。

## R4 收尾判定

R4 的代码与自动化质量门禁已完成；真实云端动作、取消/鉴权失败和 Tauri 截图继续延期，作为后续人工补证项，不阻塞 R5 首批设计与实现。R4 仍不标记为“人工证据全部完成”。
