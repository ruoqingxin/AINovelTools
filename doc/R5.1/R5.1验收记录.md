# R5.1 验收记录

- 日期：2026-09-14
- 阶段状态：自动验收通过，人工巡检待补
- 来源：`doc/总/ADR-002渐进式共创体验修订.md`

## 当前结果

- [x] 建立 R5.1 开发流程、Migration 清单和风险基线。
- [x] 确认运行时 schema version 与 R5 关闭记录的历史差异。
- [x] 写作入口与准入解除。
- [x] 规划项显式状态与候选语义。
- [x] 正文提取候选首版：证据锚点、编辑、延期、拒绝，以及实体/事实采用。
- [x] 相关上下文切片：正式设定、知识、关系、信念、伏笔、分卷规划、来源哈希和版本限制。
- [x] 作品级剧情讨论首版：全书、分卷、章节、场景、选区范围，消息不可变，候选可晋升到规划待定区。
- [x] 最小闭环自动化烟测：一句话核心 -> 保存正文 -> 提取角色 -> 采用实体 -> 讨论 -> 规划待定区。
- [ ] 桌面与窄屏人工截图巡检。

## 已验证证据

- `pnpm --filter @ainoveltools/desktop check`：TypeScript 检查通过。
- `pnpm --filter @ainoveltools/desktop test -- --run`：桌面端全量 Vitest 通过。
- `pnpm --filter @ainoveltools/desktop test -- --run src/views/discussion-view.test.tsx`：会话建立、发送讨论、显式转候选、晋升和忽略通过。
- `cargo check -p ainoveltools-desktop`：Rust 与 Tauri IPC 编译通过。
- `cargo test -p novel-application --lib`：上下文与讨论合同测试通过。
- `cargo test -p novel-infrastructure --lib`：SQLite、迁移、候选、讨论和最小闭环测试通过。
- 迁移 40 升级测试：既有会话、消息和候选保留，重建后的外键可继续写入。
- `FeatureCatalog`：R5.1 渐进写作、规划状态和上下文标记为 `IMPLEMENTED`；正文提取和作品讨论标记为 `PARTIAL`，并记录未接入范围。

## 待补充证据

- 桌面与窄屏截图或人工巡检记录。
- 正文提取采用在对象创建与候选状态之间的跨两次调用事务收紧；首版已记录在已知风险。
