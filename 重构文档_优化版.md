# AINovelTools 精简与重构流程规划（优化版）

版本：V1.1  
日期：2026-08-20  
阶段：实施前确认  

## 1. 目标与边界

目标是将项目收敛为一条清晰、可维护、可恢复、可验证的小说创作主线，解决：

- 同一状态有多个真实来源；
- 同一业务有多套实现；
- UI、业务、线程和存储职责混合；
- 定稿和长期状态更新可能部分成功。

本轮采用分阶段迁移，不推翻重写。每一阶段必须能启动、回归、回滚，并保持旧工程可打开。

不在第一轮范围内：替换 Chroma 格式、替换章节文本命名、重写全部 Prompt、删除未完成迁移所需的兼容层。

## 2. 唯一业务主线

```text
启动应用
  -> 加载并规范化全局配置
  -> 创建、打开或切换小说工程
  -> 加载工程状态
  -> 编辑或生成大纲工作流
  -> 渲染小说架构
  -> 生成结构化分卷规划
  -> 生成并校验章节蓝图
  -> 选择连续可生成的目标章节
  -> 构建统一章节上下文
  -> 生成并确认 Prompt
  -> 生成章节草稿
  -> 编辑、AI 修改或一致性审校
  -> 显式保存草稿
  -> 原子定稿并更新长期状态
  -> 更新或标记章节索引
  -> 进入下一章
```

所有新增功能必须接入此主线。任何绕过服务层直接修改核心工程文件、或重复维护相同状态的入口，先停用、后迁移、最后删除。

## 3. 架构分层与依赖规则

| 层 | 职责 | 可以依赖 | 禁止依赖 |
| --- | --- | --- | --- |
| 启动与运行时 | 日志、异常、路径、全局配置、窗口创建 | 工程管理、UI | 具体业务实现细节 |
| 工程管理 | 创建、打开、切换、关闭工程 | 服务、仓储 | UI 控件、AI SDK |
| UI | 输入、展示、确认、未保存提示 | 应用服务、任务控制器 | 仓储文件操作、Prompt 拼接、线程创建 |
| 业务服务 | 业务流程和规则 | 仓储、AI 适配、领域模型 | UI 控件、文件路径拼接 |
| AI 适配 | LLM/Embedding 调用、取消、重试、响应统一 | 配置、任务取消令牌 | UI、工程文件 |
| 存储 | 路径保护、读取、原子写入、事务、快照 | 领域模型 | UI、AI 适配 |

核心模块：`ProjectManager`、`NovelProjectRepository`、`OutlineService`、`BlueprintService`、`ChapterContextBuilder`、`ChapterService`、`FinalizationService`、`KnowledgeService`、`RoleLibraryService`、`TaskController`。

## 4. 工程与数据契约

### 4.1 配置归属

全局 `config.json` 只保存应用级设置：模型预设、代理、WebDAV、界面设置、全局技能库位置、最近工程和当前工程路径。

小说相关参数保存到工程的 `project.json`：书名、题材、章节数、字数目标、当前章节、规划指导、蓝图与分卷模式、当前卷、已选技能 ID。

每一个结构化文件必须包含 `version`，并通过对应校验器读取。读取旧版本时：备份关键文件、迁移到内存、校验、原子写入；失败则保留原文件且报告错误。

### 4.2 标准工程目录

```text
小说工程/
├─ project.json
├─ outline_workflow.json              # 大纲真实源数据
├─ Novel_architecture.txt             # 大纲渲染产物
├─ volume_plan.json                   # 分卷真实源数据
├─ blueprint.json                     # 蓝图真实源数据
├─ Novel_directory.txt                # 蓝图渲染/兼容产物
├─ global_summary.txt
├─ character_state.txt
├─ plot_arcs.txt
├─ chapter_manifest.json
├─ chapters/
├─ chapter_states/
├─ 角色库/
├─ vectorstore/
└─ knowledge_sources/
```

`Novel_architecture.txt` 和 `Novel_directory.txt` 继续保留文件名以兼容旧工程，但不再作为结构化业务状态的唯一来源。

## 5. 状态模型

### 5.1 大纲步骤

步骤状态仅为：`pending`、`draft`、`confirmed`。禁用步骤不参与渲染。每个步骤包含稳定 `id`、顺序、标题、内容、来源和历史记录。

大纲生成至少要求一个启用且非空的步骤；确认内容自动纳入，非空草稿由用户在生成前确认是否纳入。AI 快速生成必须按 `step_id` 写入为 `draft`，不得覆盖最终架构文本。

### 5.2 章节状态机

`chapter_manifest.json` 是章节状态和元数据的唯一来源。章节状态为：

| 状态 | 含义 | 可执行操作 |
| --- | --- | --- |
| `missing` | 尚无章节文件 | 生成草稿 |
| `draft` | 有未定稿正文 | 编辑、保存草稿、审校、定稿 |
| `draft_modified` | 已定稿章的正文被修改 | 编辑、审校、重新定稿 |
| `finalized` | 正文和长期状态已一致提交 | 阅读、创建草稿修改 |
| `stale` | 前序重定稿导致状态过期 | 阅读、重建状态；禁止向后生成 |
| `index_pending` | 已定稿但索引失败 | 重建索引；章节仍视为已定稿 |

合法转换：

```text
missing -> draft -> finalized -> draft_modified -> finalized
finalized -> index_pending -> finalized
finalized 或 index_pending -> stale
stale -> finalized（完成对应章节的状态重建后）
```

`draft_modified` 的章节若后方已有已定稿章节，保存时标记后方章节为 `stale`；真正重新定稿后，必须从该章前一章的状态快照重建该章状态。

### 5.3 连续性规则

以 `chapter_manifest.json` 中存在且未失效的章节计算 `max_contiguous`，不能只取最大文件号：

```text
目标章 <= max_contiguous       重写已有章节
目标章 == max_contiguous + 1   允许新增
目标章 > max_contiguous + 1    禁止跳章
```

任一 `stale` 章节存在时，禁止新增章节。用户需选择逐章重新定稿或执行受控状态重建；重建从失效区间前一章的快照开始，完成后才解除生成限制。

## 6. 存储事务与恢复

所有单文件写入由仓储层原子保存。多文件定稿通过 `write_many()` 执行：

1. 校验全部待写数据和路径；
2. 在同一工程的临时目录写入所有新文件并刷盘；
3. 建立最小事务日志，记录目标文件、备份和提交状态；
4. 将原文件移入可恢复备份，再替换全部目标文件；
5. 校验提交结果，删除临时数据和事务日志。

应用启动和打开工程时必须检测未完成事务日志：自动恢复最后一个完整版本，或完成已验证的提交；无法自动判断时拒绝继续写入并提供恢复报告。

章节定稿的事务文件固定为：章节正文、全局摘要、角色状态、剧情线、章节清单、章节状态快照。向量索引不属于该事务：文件事务成功后再更新索引，失败仅标记为 `index_pending`。

## 7. 工程管理与并发

工程切换只有 `ProjectManager.switch_project(project_path)` 一个入口。创建工程、打开工程、最近工程切换都调用它。

切换流程：检查未保存内容 -> 处理保存/放弃/取消 -> 请求取消当前任务 -> 等待任务完全退出并释放工程资源 -> 关闭旧知识库与相关窗口 -> 清空旧 UI 缓存 -> 创建新仓储 -> 加载并校验完整状态 -> 刷新全部页面。

若任务在限定时间内无法结束，切换失败并保持旧工程，不允许在任务仍可能写旧工程时切换路径。全局默认单任务互斥，由 `TaskController` 管理任务 ID、取消令牌、进度、异常、按钮状态与主线程回调；UI 和业务模块不得自行创建线程。

## 8. 蓝图、上下文与 Prompt

蓝图支持全书、范围和按卷生成。范围替换必须解析为结构化数据，验证范围、章节连续性、无重复及必填字段后，才替换目标范围并原子保存。必填字段为：`chapter_number`、`chapter_title`、`chapter_role`、`chapter_purpose`、`suspense_level`、`foreshadowing`、`plot_twist_level`、`chapter_summary`。失败时保留原蓝图和模型原始输出。

所有章节生成、批量生成和旧章重写共用 `ChapterContextBuilder.build()`，统一提供架构、当前与下一章蓝图、摘要、角色状态、剧情线、历史章节、角色资料、技能与可选知识库上下文。

没有 `vectorstore` 时跳过检索且不要求 Embedding；存在向量库时才验证 Embedding。审校不使用 Embedding。角色资料和写作技能在上下文构建阶段注入，不再通过生成后的 Prompt 字符串替换。

## 9. 草稿、审校与定稿

生成和修改函数只返回正文，绝不隐式保存。草稿由 `ChapterService.save_draft()` 显式保存。编辑器脏状态只有一个真实来源：当前文本与最后一次已保存文本是否相等。

切换章节、工程、窗口关闭、生成另一章、定稿和批量生成，都通过统一的未保存确认流程处理。

审校读取当前编辑器文本，即使尚未保存；审校结果不得自动修改正文或长期状态。Prompt 确认等待必须轮询取消令牌，取消时关闭对话框、唤醒等待、恢复 UI，且不生成章节文件。

定稿前必须校验：目标章节连续性、非空正文、章节状态、旧章影响、前序快照、摘要/角色状态/剧情线的有效响应。正文哈希相同且清单已定稿时直接返回“该版本已经定稿”，不得重复推进长期状态。

## 10. 迁移与兼容

首次打开旧工程时：检测格式 -> 备份关键文件 -> 迁移 -> 校验 -> 原子写入新文件。迁移失败恢复备份，并保留可诊断日志。

迁移来源包括：全局配置中的小说参数迁入 `project.json`；旧架构文本迁入工作流；旧章节文件生成清单；旧技能设置迁为技能 JSON 与工程选中 ID。稳定版本发布后才移除迁移代码。

第一轮保留 `architecture_sections.py`、旧配置字段读取、角色文本格式、章节命名、Chroma 格式、架构和目录文本文件名。

## 11. 测试与可观测性

每个阶段必须新增或更新以下测试：

- 单元测试：配置与迁移、状态转换、连续性计算、蓝图校验、模型参数构造；
- 集成测试：工程切换、草稿保存、定稿事务、索引失败、无知识库生成；
- 故障注入：写入失败、事务中断、LLM 空响应、取消 Prompt 等待；
- UI/端到端测试：未保存确认、任务互斥、切换完整刷新。

日志必须包含任务 ID、工程 ID、章节号和异常上下文；不得记录 API Key。原始模型输出仅在调试模式保存，日志需要大小上限和轮转策略。每个阶段验收应提供可执行测试命令及最小手工回归路径。

## 12. 分阶段实施计划

### 阶段 0：重构基线

备份、建立分支、记录旧格式、建立最小测试工程和关键流程日志。验收：可启动，并至少完成一次架构、蓝图、章节生成。

### 阶段 1：修复确认错误

统一后台按钮传递；合并章节脏状态；修复保存返回值；支持 Prompt 取消；审校读取编辑器文本；按向量库存在性检查 Embedding；移除审校 Embedding 检查；修复草稿保存展示；批量扩写使用真实最低字数。此阶段不修改文件格式和定稿模型。

### 阶段 2：统一基础逻辑

统一任务模型配置、LLM/Embedding 参数、后台任务入口、角色加载、技能合并和 UI 主线程更新；核验引用后清理 `ui/helpers.py` 与明确死代码。验收：UI 不再自行创建重复 AI 线程，单章与批量使用相同配置。

### 阶段 3：工程管理和多小说切换

引入 `project.json`、`ProjectManager` 和完整 `switch_project()`；实现未保存与任务并发规则。验收：A/B 工程完全隔离，重启可恢复最近工程。

### 阶段 4：大纲唯一状态源

工作流成为唯一源，支持未完成 34 步生成，快速生成导入草稿，旧架构迁移后停止 UI 的旧直接编辑入口。验收：删除渲染文件后可由工作流稳定重建。

### 阶段 5：分卷与蓝图服务

引入 `volume_plan.json` 和 `blueprint.json`，实现分卷校验、范围蓝图生成与安全替换。验收：无越界、重复、缺章或范围外覆盖。

### 阶段 6：统一章节服务

实现上下文构建器、统一草稿接口、连续性校验和章节清单。验收：只能连续新增，单章与批量上下文一致。

### 阶段 7：原子定稿与状态快照

实现多文件事务、恢复日志、快照、哈希幂等、失效标记和索引重建。验收：任何状态生成失败都不提交孤立正文；旧章重定稿后不能继续基于过期状态生成。

### 阶段 8：角色库、技能和 WebDAV 精简

“全部”改为虚拟视图，“未分类”为真实目录；统一角色原子保存；工程保存技能选择；WebDAV 改为仅备份/恢复全局配置并在恢复后完整刷新 UI。

### 阶段 9：删除旧代码与模块拆分

迁移完成、静态检查和回归通过后，删除不可达旧入口与兼容 UI，拆分过大的 UI 模块。任何删除前必须完成引用扫描和迁移验证。

## 13. 执行纪律与最终验收

每阶段固定执行：确认范围 -> 扫描引用 -> 更新测试 -> 小范围修改 -> 静态检查 -> 自动化测试 -> 手工验证 -> 单职责提交。禁止跨阶段同时重构 UI、存储和核心业务流程。

最终端到端验收必须覆盖：未完成大纲生成架构、六卷规划、范围蓝图、草稿编辑与审校、前三章连续定稿、跳章拦截、重写第二章导致后续失效、A/B 工程切换与重启恢复、无知识库生成、有知识库检索、LLM 和 Prompt 取消、写入/状态/索引失败恢复，以及重复定稿幂等性。
